# Overrides for the kitty-based Herdr launcher (HerdrKitty.desktop), layered
# on top of the regular config to match the ghostty herdr look. Add future
# herdr-only tweaks here instead of piling -o flags on the Exec line.
include kitty.conf

font_size 14.0
background_opacity 0.9
background #000000
