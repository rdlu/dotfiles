# daisy-only mise tools. Merged additively on top of ~/.config/mise/config.toml.
# Only present on daisy (this overlay is stowed only there).
[tools]
"cargo:probe-rs-tools" = "latest"  # microcontroller side project
